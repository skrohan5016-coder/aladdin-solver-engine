# Recorded shadow data — never commit this
/data/
*.jsonl

# Local tuning. This is where the actual edge lives; keep it off GitHub.
config.local.*
.env

# Build output
/bin/
/solver
/report
/replay
/contractcheck

# Private sealed replay corpora
/corpora/
/private-corpus/

# Python helper caches
__pycache__/
*.py[cod]
